# 문제 수정 기능 설계

## 1. 개요
관리자가 입력한 문제를 조회하고 수정할 수 있는 기능을 구현합니다.

## 2. 기능 요구사항

### 2.1 문제 목록 조회
- 관리자 화면에서 등록된 모든 문제를 목록으로 조회
- 필터링: 자격증 유형, 카테고리, 검색어
- 페이지네이션 (선택사항)
- 정렬: 최신순, 오래된순, 카테고리순

### 2.2 문제 상세 조회
- 문제 ID로 특정 문제의 상세 정보 조회
- 문제 제시문, 선택지, 정답, 해설 등 모든 정보 표시

### 2.3 문제 수정
- 기존 문제 정보를 폼에 로드
- 수정 후 저장
- 수정 전/후 비교 (선택사항)

### 2.4 문제 삭제 (선택사항)
- 문제 삭제 기능
- 삭제 전 확인 다이얼로그

## 3. 화면 구조

### 3.1 문제 목록 화면 (`AdminQuestionList.tsx`)
```
/admin/questions
- 문제 목록 테이블/카드 형태
- 검색 및 필터 기능
- 각 문제에 "수정" 버튼
- 각 문제에 "삭제" 버튼 (선택사항)
```

### 3.2 문제 수정 화면 (`AdminQuestionEdit.tsx`)
```
/admin/questions/:id/edit
- 기존 QuestionInputForm 재사용
- initialData에 문제 데이터 전달
- 저장 시 update API 호출
```

## 4. API 함수 설계

### 4.1 문제 목록 조회 (`getQuestionsList`)
```typescript
export async function getQuestionsList(options?: {
  certificationType?: string
  category?: string
  search?: string
  limit?: number
  offset?: number
  orderBy?: 'created_at' | 'category' | 'difficulty'
  order?: 'asc' | 'desc'
}): Promise<{
  questions: QuestionListItem[]
  total: number
  error?: string
}>

interface QuestionListItem {
  id: string
  content: string // 미리보기 (50자 제한)
  certificationType: string
  category: string
  difficulty: number
  createdAt: string
  optionsCount: number
}
```

### 4.2 문제 상세 조회 (`getQuestionById`)
```typescript
export async function getQuestionById(questionId: string): Promise<{
  question: QuestionDetail | null
  error?: string
}>

interface QuestionDetail {
  id: string
  content: string
  subContent?: string
  subContentImageUrl?: string
  options: string[]
  correctAnswer: string // A, B, C, D, E
  explanation: string
  certificationType: string
  category: string
  difficulty: number // 1-5 (상=5, 중=3, 하=1)
  tags: string[]
  frequency?: number
  createdAt: string
}
```

### 4.3 문제 수정 (`updateQuestion`)
```typescript
export async function updateQuestion(
  questionId: string,
  question: QuestionInput
): Promise<{ id: string | null; error: string | null }>
```

### 4.4 문제 삭제 (`deleteQuestion`) - 선택사항
```typescript
export async function deleteQuestion(
  questionId: string
): Promise<{ success: boolean; error?: string }>
```

## 5. 컴포넌트 구조

### 5.1 AdminQuestionList 컴포넌트
```typescript
// src/pages/admin/AdminQuestionList.tsx
- 문제 목록 표시
- 검색/필터 UI
- 각 문제에 수정/삭제 버튼
- 페이지네이션 (선택사항)
```

### 5.2 AdminQuestionEdit 컴포넌트
```typescript
// src/pages/admin/AdminQuestionEdit.tsx
- QuestionInputForm 재사용
- 문제 ID로 데이터 로드
- 수정 후 저장
```

### 5.3 QuestionInputForm 수정
```typescript
// 이미 initialData prop이 있으므로 수정 모드 지원 가능
// mode?: 'create' | 'edit' prop 추가 (선택사항)
// 수정 모드일 때 "수정" 버튼, 생성 모드일 때 "저장" 버튼
```

## 6. 라우팅 구조

```typescript
// App.tsx에 추가
<Route
  path="/admin/questions"
  element={
    <AdminProtectedRoute>
      <AdminQuestionList />
    </AdminProtectedRoute>
  }
/>
<Route
  path="/admin/questions/:id/edit"
  element={
    <AdminProtectedRoute>
      <AdminQuestionEdit />
    </AdminProtectedRoute>
  }
/>
```

## 7. 데이터베이스 쿼리

### 7.1 문제 목록 조회
```sql
SELECT 
  id,
  content,
  certification_type,
  category,
  difficulty,
  created_at,
  jsonb_array_length(options) as options_count
FROM questions
WHERE 
  ($1::text IS NULL OR certification_type = $1)
  AND ($2::text IS NULL OR category LIKE $2 || '%')
  AND ($3::text IS NULL OR content ILIKE '%' || $3 || '%')
ORDER BY created_at DESC
LIMIT $4 OFFSET $5;
```

### 7.2 문제 상세 조회
```sql
SELECT * FROM questions WHERE id = $1;
```

### 7.3 문제 수정
```sql
UPDATE questions
SET 
  content = $2,
  options = $3,
  correct_answer = $4,
  explanation = $5,
  certification_type = $6,
  category = $7,
  difficulty = $8,
  tags = $9,
  sub_content = $10,
  sub_content_image_url = $11,
  frequency = $12,
  updated_at = NOW()
WHERE id = $1;
```

### 7.4 문제 삭제
```sql
DELETE FROM questions WHERE id = $1;
```

## 8. RLS 정책 추가 필요

### 8.1 UPDATE 정책
```sql
CREATE POLICY "Allow admin update questions"
ON questions FOR UPDATE
TO authenticated
USING (
  EXISTS (
    SELECT 1 FROM users
    WHERE users.id = auth.uid()
    AND users.is_admin = true
  )
)
WITH CHECK (
  EXISTS (
    SELECT 1 FROM users
    WHERE users.id = auth.uid()
    AND users.is_admin = true
  )
);
```

### 8.2 DELETE 정책 (선택사항)
```sql
CREATE POLICY "Allow admin delete questions"
ON questions FOR DELETE
TO authenticated
USING (
  EXISTS (
    SELECT 1 FROM users
    WHERE users.id = auth.uid()
    AND users.is_admin = true
  )
);
```

## 9. 구현 순서

1. **API 함수 구현**
   - `getQuestionsList` 구현
   - `getQuestionById` 구현
   - `updateQuestion` 구현
   - `deleteQuestion` 구현 (선택사항)

2. **데이터베이스 정책 추가**
   - UPDATE 정책 추가
   - DELETE 정책 추가 (선택사항)

3. **컴포넌트 구현**
   - `AdminQuestionList` 컴포넌트 생성
   - `AdminQuestionEdit` 컴포넌트 생성
   - `QuestionInputForm`에 수정 모드 지원 추가

4. **라우팅 추가**
   - `/admin/questions` 라우트 추가
   - `/admin/questions/:id/edit` 라우트 추가

5. **네비게이션 추가**
   - 문제 입력 화면에 "문제 목록" 버튼 추가
   - 문제 목록 화면에 "문제 추가" 버튼 추가

## 10. UI/UX 고려사항

- 문제 목록은 카드 형태 또는 테이블 형태로 표시
- 검색/필터는 상단에 배치
- 수정 버튼 클릭 시 수정 화면으로 이동
- 수정 완료 후 목록으로 돌아가기 또는 수정 화면 유지
- 로딩 상태 표시
- 에러 처리 및 사용자 피드백

